<div class="col-md-4 mt-5 text-center">
    <div class="card " >
        <img src="{{asset('../images/dishes/')}}{{image.file}}" class="rounded" style="height: 12rem;" alt="...">
        <div class="image-info"><h5>{{image.nom}}</h5></div>
    </div>
</div>

