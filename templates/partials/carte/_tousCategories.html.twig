
<div class="row mt-5" >
    <div class="carteCategoryTitel text-center">
        {{categorie.nom}}</h6>
    </div> 

    <div class="row m-0 p-0">
        {% for plat in plats %}
            {% if categorie.nom == plat.categories  %}
                <div class="row mb-3 p-0">
                    <div class="col-md-10">
                        <h6 class="hCarte mb-2">{{plat.nom| raw}}</h6>
                        <div class="pCarte">
                            {{plat.description| raw}}
                        </div>
                    </div>
                    <div class="col-md-2 prix">
                        {{plat.prix| raw}} €
                    </div>
                </div>                        
            {% endif %}
        {% endfor %}           
    </div>
</div>